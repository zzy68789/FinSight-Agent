# 待实现功能

更新时间：2026-08-20

> 本文件记录 FinSight Agent 收束为 **A股/ETF 金融投研 Agent** 后仍未完成或需要增强的能力。
> 已完成能力见 `docs/目前已经实现的功能.md`，遗留问题见 `docs/待解决问题.md`，踩坑日志见 `docs/PROBLEM_LOG.md`。
> `.ztemp/` 只作为临时日志、环境备份和归档计划目录，本轮不回写。

## 优先级总览

| 优先级 | 方向 | 关键待办 | 验收标准 |
| --- | --- | --- | --- |
| P0 | Agent 真实回归 | 真实 LLM 动态计划、不同问题工具序列、真实 Provider 补证据 Trace | 联网任务证明问题会改变动作，补证据产生可用新增事实而非只改变 query |
| P0 | Agent 恢复与安全 | Durable Turn/outbox 已实现，补真实 MySQL 故障注入、V1→V9 迁移和外部调用幂等验证 | 已提交 turn 不重复外部调用，过期 lease 不能写 journal/完成任务，不绕过最终门禁 |
| P1 | Planner 性能基线 | 已持久化模型、Token、合法率和 P50/P95，补真实流量样本与路由阈值校准 | FAST/SMART 在质量不退化前提下降低常规动作延迟与 Token |
| P1 | 数据源硬化 | TuShare 真实 token 联调、缓存、限速、接口权限错误提示 | 外部接口失败不会耗尽线程，证据缺失原因可追踪 |
| P1 | 可比证券校准 | 独立取证与确定性比较已实现，补真实股票/ETF 样本、报告门禁和浏览器联调 | 主/可比数据不串用，缺失项可解释，真实报告引用可回到所属证券 |
| P1 | 评测与 Reviewer | 扩充真实 Golden Set、无单位/区间数字表达、评测趋势管理 | 离线回归持续吸收 Bad Case，并能逐项定位全文数字与证据偏差 |
| P1 | 前端投研体验 | 安全渲染、任务分页、懒加载与基础移动端回归已完成，补段落级 Bad Case、长轨迹筛选和真实下载 | 在已独立的研究页内完成更细粒度复核、反馈与可靠导出 |
| P1 | 提交恢复体验 | 幂等创建、刷新恢复、显式重连和安全取消已实现，补真实浏览器断网/刷新故障注入 | 网络不确定时不重复建任务，刷新后恢复同一 task 与缺失事件 |
| P2 | 金融垂直增强 | ETF 持仓/跟踪误差/申赎清单、股票行情序列、高级技术指标 | 在现有 ETF 净值/K 线/多空角色基础上补齐深层数据 |
| P2 | 运维与配置 | `.env.example`、traceId、外部依赖健康检查细化、数据库 CI | 本地启动配置可复制，故障能通过日志与健康检查定位 |
| P2 | 事件投递运维 | 死信告警/重放入口、按任务轮询压力基线与自适应策略 | 投递失败可见且可恢复，多实例高并发下查询与延迟有明确容量边界 |
| P2 | 报告与权限 | 报告精排、知识库运营能力、细粒度 RBAC | 报告复用和管理能力满足多用户隔离要求 |
| P3 | 数据库演进 | Docker/CI 实跑 Testcontainers，扩展旧库 baseline 与 Mapper 覆盖 | 空库初始化和旧库 baseline 升级均有实际自动回归证据 |

---

## P0：重构收口

### 前端通用研究模式清理

当前状态：已完成。Run 工作区已经删除“通用研究”双模式选择和自由主题输入，生成按钮直接调用 `startStockResearch`；`frontend/src/services/api.js` 不再导出 `streamChat`，旧固定 `StatusFlow` 已由 canonical 事件驱动的 `ResearchMissionControl` 取代，报告库中依赖旧 `/api/chat` 的“修订”按钮也已移除。登录页、任务详情和运行文案同步收敛为 A股/ETF 金融投研定位。

后端核对：通用 `ChatController`、`ResearchGraphFactory`、通用 `agent/node` 和 `ResearchTaskService` 此前已经删除，本轮继续移除最后一个无引用的 `ChatRequest`。`ResearchTaskMapper`、PDF/RAG、上传和知识库清理能力仍被 `/api/stock-reports` 使用，因此按实际依赖保留。

验证：2026-07-15 静态检索确认前端生产代码不再包含 `/chat`、`streamChat`、`runMode` 或通用研究流程；`npm.cmd run build` 完成 1798 个模块转换并构建成功；后端 `mvn.cmd test` 结果为 129 tests, 0 failures, 0 errors, 2 skipped，其中新增测试明确断言 `/api/chat` 返回 404。

### 项目标识统一

当前状态：已完成。Java 根包、Spring 应用名、Maven 坐标、配置命名空间、MySQL、Redis key、Chroma collection、前端存储键、README、关键页面文案、浏览器标签页与主页面顶栏品牌图标均统一为 FinSight；专属 SVG 使用研究镜头、金色趋势线和深海军蓝底色。只有三类 API Key 使用 `API_KEY`、`TAVILY_API_KEY`、`TUSHARE_API_KEY` 环境变量，其余配置保持简洁直写。

验证：2026-07-11 后端 91 个测试通过；真实 MySQL `finsight` 空库完成 Flyway V1→V2；2026-07-15 专属 favicon 和前端金融单入口均完成生产构建验证。

### Java 中文注释规范

当前状态：已完成。141 个主代码 Java 文件均包含简洁的中文类级 Javadoc，核心 Service 契约与金融业务方法已补充方法级说明；30 个 Controller 接口方法、51 个公开 `record` 的 299 个组件字段及 9 个普通 DTO 字段均有中文说明。`ArchitectureLayeringTest` 和 `DocumentationConventionTest` 会阻止缺少类、接口方法或数据字段说明的新代码进入项目。后续只需随业务改动维护职责说明，不单独安排批量补注释任务。

---

## P0：Research Agent 可靠性

### Agent Runtime 编排测试

当前状态：固定 `StockReportRunner` 已删除。Replan LLM 现在直接接收当前计划、最近观察、门禁问题、补证据尝试和证据增量；`ResearchAgentRuntimeTest` 覆盖 Planner 主动停止、数字门禁重算和补证据后 PASS 完整链路，`ResearchPlannerTest` 覆盖反馈感知 Replan 与 FAST→SMART 结构失败升级。工具使用不可变上下文、类型化 `ToolPayload` 和集中状态 reducer。补证据恢复有独立预算，`ProgressFingerprint` 能在计划问题、证据、指标和门禁路由连续不变时停止。

剩余：使用真实 LLM、Tavily/TuShare 和数据库任务执行两组差异化研究问题，并保存 `/api/research-runs/{taskId}/trace`；验证模型确实使用门禁反馈改变 required evidence/tool 参数，新 query 能产生与缺口相关的有效证据，并据真实样本校准 `maxEvidenceRecoveries` 与 `maxStagnantTurns`。

### Checkpoint 断点恢复

当前状态：`DurableTurnCommitModule` 已在一个事务内写入 Planner 动作、工具结果、证据/快照/指标、turn、`agent-state-v3-lite` checkpoint、事件 outbox 和 lease 心跳；最终 PASS 会原子写报告版本、冻结快照、完成任务和 `run_completed`。`openTurn` 锁定当前租约行，工具开始、turn 提交和 finish 都在 SQL 写入点校验 `lease_owner + lease_epoch`。恢复遇到未提交 `PLANNED` turn 时会复用原始完整动作与 Planner 元数据；`AgentStateStore` 可迁移 v1/v2、加载重对象并从已提交 journal 重建调用索引，未知版本和损坏状态 fail-closed。

待实现：在 Docker 可用环境增加真实事务故障注入，覆盖 Planner 动作、工具结果、证据、指标、报告、快照冻结、checkpoint 和 outbox 写点，确认整轮/最终完成事务回滚；验证 V1→V9 迁移、旧 `lease_epoch` 无法 open/start/commit/finish、重复恢复和 outbox 重发。外部工具已经执行但数据库事务尚未提交的窗口仍只能依赖幂等 Provider 与后续对账，不能宣传 exactly-once。

### Planner 模型路由与性能基线

当前状态：`PlannerModelPolicy` 已实现 create/replan 用 SMART、普通 nextAction 用 FAST、补证据/门禁恢复与 FAST 结构失败升级 SMART；`agent_planner_call` 保存决策类型、请求/实际模型、路由原因、结构合法性、Token 和耗时，Trace 返回分模型/决策成功率及 P50/P95。当前只有单元测试和本地 fallback 数据，尚没有可代表线上质量的非零真实模型样本。

待实现：对固定股票/ETF问题集连续运行真实模型，分别统计 create/replan/ordinary/recovery 的结构合法率、路由正确率、降级率、Token、P50/P95 和最终门禁通过率；只有常规动作质量不下降时才继续扩大 FAST 使用范围。

### LLM 报告 Writer

当前状态：Writer 延迟优化已有历史重放结果；紧凑有效证据索引、正文 `[E#]` 就近引用、阶段性 ROE 未年化口径、结构化估值优先、真实缺失项汇总和来源仲裁附录已纳入回归。Writer 版本已升级为 `investment-report-writer-v7-source-arbitration`，仍需重启应用后确认真实 LLM 能稳定保留多空子节、新引用规则和来源仲裁说明且延迟不明显回升。

### Mapper 层集成测试

当前状态：Testcontainers/MySQL 8.4 用例保留 `agent_turn`、`agent_tool_call`、AgentState Checkpoint、任务租约 CAS、Planner-turn 关联/outbox 表、任务创建幂等约束、证据证券归属字段和 `ReportMapper` PASS 复用/软删除/owner 隔离。本轮代码已增加 Flyway V9，但 Docker 不可用，因此 V1→V9 尚未取得容器实跑证据。

待实现：

- 在 Docker 可用本机或 CI 实跑现有 Testcontainers 用例。
- 继续覆盖 `FinancialSnapshotMapper`、更多 TypeHandler JSON 字段和旧 V1 baseline 升级。
- 增加重复启动不重复迁移及更多 owner 隔离查询断言。

验收标准：

- 集成测试能发现列名错、ResultMap 错、TypeHandler JSON 格式错。
- 任务失败时 `research_task.status` 能正确变为 `FAILED`。

### Agent 生命周期收口

当前状态：新建、重试和过期恢复统一进入 `DurableAgentRunner`；固定 Workflow、Runner 和旧恢复调度已删除。任务状态、轮次、工具数、停止原因、策略版本和完成时间均由 Agent Runtime 落库。用户取消会原子写入 `CANCELLED`、递增 lease/event sequence 并提交 `run_stopped` outbox，旧执行器不能再提交 turn、完成任务或发布报告；重复取消幂等。

### Agent Event Delivery 与 Projection

当前状态：后端 `agent_event_outbox` 已提供版本化事件、任务内单调 sequence、数据库 claim/过期接管、指数退避和死信状态；`AgentEventStreamModule` 处理回放/实时竞态并轮询共享数据库补齐其他实例事件，`GET /api/research-runs/{taskId}/events` 支持 `Last-Event-ID`/`afterSequence` 续传。Trace 与 SSE 读取同一 canonical event；前端 `agentEventProjection.js` 按 `taskId + sequence` 幂等，增量 SSE parser 与有限重连保留最后 sequence，`useAgentRunProjection` 把运行态和回放从 `App.vue` 抽离。

待实现：为死信数量、重试次数和发布延迟增加 Micrometer 指标、告警与受控重放入口；用多实例并发和慢消费者压力测试量化当前“每个活跃任务每秒一次共享数据库查询”的容量边界，再决定是否增加通知通道或自适应轮询。前端已完成 Launcher、Mission Control、报告面板、运行检查器、断线提示和手动重连；剩余工程项是长时间线筛选，以及真实慢消费者/断网条件下的恢复验收。

### 任务创建幂等与刷新恢复

当前状态：`POST /api/research-runs` 已先以 `Idempotency-Key` 创建持久化任务并返回 202 回执，再由独立 GET SSE 订阅；V8 使用用户级唯一约束收口并发重复创建。前端保存 pending 提交意图和活动 task/thread/sequence，首次 SSE 失败可从事件接口回放，刷新会先加载 Task + Trace 再续订缺失序号。自动重连耗尽或恢复查询暂时失败时会保留任务指针，用户可显式重连、进入任务中心或取消后台任务。

待实现：启动真实 MySQL 与后端，在浏览器中分别注入“POST 响应丢失”“任务已创建但首次 GET SSE 失败”“收到中间事件后刷新”“终态后刷新”四类故障，确认数据库始终只有一个任务且页面投影与完整 Trace 一致；V9 Testcontainers 用例需在 Docker/CI 实跑。后续多会话阶段应把当前持久 thread 从浏览器键升级为后端 Research Session，不把这一本地恢复指针误称为会话记忆。

### 可比证券：真实数据与报告联调

当前状态：`comparison_tickers`、同类证券校验、独立快照/证据归属、Planner 采集守卫、股票/ETF 确定性比较指标、`DATA_MISSING` 和证券归属引用已经完成；前端只在后端能力具备后开放最多 3 个候选。单元测试已覆盖代码规范化、主证券排除、跨资产拒绝、主/可比指标隔离、缺失降级和报告归属。

待实现：使用真实 TuShare token 分别运行“贵州茅台 + 五粮液”和“两只同类 ETF”任务，核对 PE/PB、营收同比、净值和折溢价口径；在浏览器确认选择、任务回放、报告正文和证据检查器均显示正确证券归属。后续若增加行业自动推荐，必须来自可审计的行业分类和流动性规则，不能让 LLM 凭名称猜同行。

验收标准：真实 Trace 中每个可比证券只产生自己的 `comparisonSnapshotId`，数据库证据归属与报告 `[可比 代码]` 一致；某一证券数据为空时其他证券仍完成，报告明确保留 `DATA_MISSING`。

---

## P1：数据源硬化

### TuShare Provider 真实联调

当前状态：`TushareMarketDataProvider` 已接入 `income`、`balancesheet`、`cashflow`、`daily_basic` 和 ETF `fund_daily` 映射；财报版本去重、严格同期匹配、年初/期末权益 ROE 口径及基础语义校验已完成。`finsight.market.tushare.timeout` 已真正用于连接/读取超时，Provider 聚合层另有统一超时与取消降级；未配置 token 时不访问外部接口。

待实现：

- 使用真实 TuShare token 联调普通股票和 ETF 样例。
- 补充 provider 级缓存，避免同一任务/同一报告期重复请求。
- 增加接口限速、权限不足、额度不足、数据为空的分类错误提示。
- 验证指定报告期下当前期与上年同期两次查询的额度消耗，并评估合并请求或短期缓存。

验收标准：

- 外部接口超时会在配置时间内失败，并写入 `DATA_MISSING` 证据。
- provider stage 能展示失败类型、耗时和证据数量。
- 有针对 TuShare 超时和错误响应的单元测试。

### 多数据源质量打分

当前状态：已写入 `TavilyExtractClient` 和公开网页质量门控；2026-08-12 新增 `FinancialEvidenceSourcePriorityPolicy`、`FinancialEvidenceConflictResolver` 与快照内 `FinancialEvidenceArbitration`。同指标同报告期数值按绝对 0.01 / 相对 0.5% 容差和固定来源层级选出唯一输入；同级未决冲突产生 `EVIDENCE_CONFLICT` 并回到补证据，补采集后会重仲裁。最终报告的“证据来源仲裁”已展示采用来源、数值、原因与策略版本，机制级验收已经满足；真实联网来源分布与权重仍需数据验证。

待实现：

- 使用真实 TuShare、交易所公告、正式媒体和 Tavily fallback Bad Case 校准当前来源权重与 0.5%/0.01 容差。
- 对新闻正文增加发布时间解析、时效性和跨来源事实去重。
- 在独立研究页增加结构化仲裁候选和来源优先级的可视化筛选；当前 Markdown 报告已经展示仲裁结论。

验收标准：

- 真实联网任务中，同一指标多来源冲突时仍能稳定说明采用哪个来源及原因；同级冲突必须补证据或停止。

---

## P1/P2：评测与 Reviewer

### 独立批量评测 Runner

当前状态：已完成首版。`FinancialEvaluator` v5 对所有证券执行线上硬门禁并把来源可用性降为告警；`FinancialEvaluationRunner` 使用 12 个股票/ETF 冻结正例、8 个规则负例和 24 个检索问题，输出 JSON/Markdown，执行 Recall@K、Precision@K、MRR、NDCG@K、数值误差和基线回归。PR GitHub Actions 已接入确定性强门禁，Judge/真实数据工作流保持非阻断。

验证：2026-07-15 执行 `mvn.cmd "-Dtest=FinancialEvaluationRegressionTest" test` 通过；随后后端全量 128 项测试通过，Judge 和真实数据冒烟因默认关闭跳过 2 项。

剩余可增强：

- 把已确认的生产 Bad Case 脱敏后持续加入 `dataset-v1`，扩大行业、报告期和异常数据覆盖。
- 在积累多次 Judge/真实数据结果后建立非零模型质量、P95 延迟和 Token 基线。
- 后续再建设数据库趋势存储和评测管理页面；首版不增加线上 API 或表结构。

### 报告数字全量抽取与误差校验

当前状态：已完成首版。`ReportNumericFactVerifier` 抽取正文中的百分比、倍数和金额，统一换算元/万元/亿元，并按 0.5% 相对误差或 0.01 绝对误差与 `FinancialSnapshot` / `FinancialMetricResult` 逐项比对；`CitationReviewer` v4 对无法追溯的数字 fail-closed，`FinancialEvaluator` v5 将全量声明纳入 numeric consistency。

待实现：

- 继续扩展无单位价格、区间、每股指标和带中文数字表达。
- 从真实 Bad Case 评估误报率，再决定是否引入指标语义/引用范围约束。

验收标准：

- LLM 或模板写错关键数字时，reviewer 能 fail-closed。
- 报告中每个关键财务数字都有证据或指标结果来源。

---

## P1：前端投研体验

### 独立股票报告页

当前状态：首版已完成。新增 `/reports/:reportId` 独立股票/ETF 研究页，报告库可直接进入；页面汇合报告、同 thread 版本、任务回放和 Agent 日志，已提供版本时间带、逐行对比、证据详情、来源/关键词筛选、正文 `[E#]` 锚点、原始来源跳转，以及桌面三栏/移动纵向布局。三个报告入口已共用禁用原始 HTML、仅允许锚点与 HTTP(S) 的安全 Markdown；当前 Run 只在最终提交事件后展示并开放导出，报告库继续保留三种快捷入口。

剩余可增强：

- 增加风险维度筛选和更长时间范围的报告时间线筛选。
- 把 Bad Case 反馈入口放到报告具体段落或证据旁边。
- 为版本差异增加章节级摘要，避免长报告只能逐行浏览。
- 使用真实后端报告验收 PDF/Word/Markdown 下载、失败提示和键盘关闭菜单；本地模拟 API 下的 390×844 基础布局无横向溢出已验证。

验收标准：

- 用户可以从独立页面查看正文、证据、ETF 行情、多空研究和比较版本。
- 页面在移动端和桌面端均无文本溢出。

### ECharts 行情图

当前状态：ETF 首版已完成。`FinancialSnapshot` 保存最近 60 个交易日 OHLC、涨跌幅、成交量和成交额序列；独立研究页通过按路由懒加载的 ECharts 展示 K 线、收盘线、成交额和单位净值基准，无数据时明确显示空状态。

剩余可增强：

- 为普通股票接入 TuShare `daily` / `weekly` 行情序列。
- 增加均线、RSI 等确定性 Java/前端指标及区间切换。
- 不引入 Python、akshare、matplotlib。

验收标准：

- 股票/ETF 报告页可查看对应时间范围的行情图。
- 图表数据来源可追踪到 provider 和报告期。

---

## P2：金融垂直增强

### ETF 深度数据

当前状态：首版深度快照已完成。ETF 聚合 TuShare `fund_daily`、`fund_basic` 和 `fund_nav`，保存 60 日行情、基金资料、业绩比较基准、单位/累计净值、资产净值和同日折溢价；ETF 指标增加单位净值与折溢价率，深度接口失败按项写入 `DATA_MISSING`。持仓、跟踪误差和申赎清单仍未接入。

剩余可增强：

- 接入 ETF 持仓、跟踪指数收益序列、跟踪误差和申赎清单。
- 明确 `total_netasset` 展示单位并用真实 token 校验不同基金类型口径。
- 对高权限接口增加独立开关、缓存和限速，避免权限不足拖慢基础行情。

验收标准：

- ETF 报告不套用上市公司财务指标。
- ETF 缺失数据以 `MISSING_INPUT` / `DATA_MISSING` 清晰呈现。

### 多空条件工具增强

当前状态：首版已完成。确定性 `BullBearCaseBuilder` 由 `build_bull_bear_cases` 工具调用，读取同一指标/风险快照并输出绑定 `[E#]` 的正反条件；它不再被命名或宣传为独立 Agent。

剩余可增强：

- 增加独立风险裁判角色，对多空重复观点和证据冲突做结构化仲裁。
- 在保持数字由 Java 计算的前提下，评估 LLM 对论据表达的增益与额外延迟。
- 扩充负向阈值与 ETF 跟踪误差规则，沉淀真实多空 Bad Case。

验收标准：

- 报告能展示有证据绑定的多空观点，而不是无依据结论。
- 违规表达会被合规 reviewer 拦截。

---

## P2：运维与配置

### 本地配置模板

当前状态：README 已列出主要环境变量，但还没有 `.env.example` 或 `application-local.yml.example`。

待实现：

- 新增 `.env.example`，集中列出 MySQL、Redis、ChromaDB、LLM、Tavily、TuShare、认证配置。
- 对敏感变量只给占位符，不写真实密钥。
- README 链接到模板文件。

验收标准：

- 新开发者能按模板启动本地后端。
- `ConfigurationPlaceholderTest` 继续防止回退读取全局 `OPENAI_API_KEY`。

### 可观测性与健康检查

当前状态：已接入 Actuator、Micrometer Prometheus、Agent 执行器 active/queued/rejected、任务结果、步骤耗时和恢复记录；新增登录用户可访问的 `/api/system/health` 和动态设置页，可展示 MySQL/Redis 连通性以及 ChromaDB、LLM、Tavily、TuShare 配置判定。外部 API 真实探测、traceId 和请求日志仍不完整。

待实现：

- 增加请求 traceId / MDC。
- 健康检查细化到 MySQL、Redis、ChromaDB collection、LLM API、Tavily API、TuShare API。
- 健康检查失败至少记录 debug 日志和失败原因。

验收标准：

- 出错时能通过 traceId 定位同一次请求的关键日志。
- 管理员后台能看到关键依赖的真实可用性。

---

## P2：报告、权限与数据隔离

### 报告精排与版本对比

当前状态：在线报告已收敛为单一发布链：只有最终 PASS 事务提交后才通过 `run_completed` 发布 `reportId`，失败草稿不再进入普通报告库；当前 Run、独立研究页和报告库均从同一 `reportId/version` 导出，导出服务不调用 LLM、不改变报告状态。Markdown/PDF/Word 已可用，但 PDF/Word 样式仍偏基础，真实浏览器下载尚未验收。

待实现：

- 优化 PDF/Word 目录、页眉页脚、表格、引用格式。
- 优化现有网页逐行版本对比，并让导出的 PDF/Word 携带版本元数据。
- 按 ticker、项目、标签、时间范围分类报告。

验收标准：

- 导出的报告适合投递项目展示和面试讲解。

### 知识库按用户隔离

当前状态：核心隔离已完成。PDF 上传、报告加入 RAG、BM25/内存向量检索、ChromaDB 查询和清理均绑定当前 `ownerId` 对应的 `RagKnowledgeSpace`；Chroma 通过空间化记录 ID 与 `knowledge_space` metadata 防止跨用户冲突和检索。PDF 上传改为不同来源追加、同名来源整体替换，新任务未上传文档时不再静默清空；前端提供带确认的显式清空入口。双用户内存隔离、同名来源替换、Controller 传递、Provider 检索、报告入库和 Chroma 过滤测试继续保留。

剩余可增强：

- 管理员后台可按用户检查知识库占用与清理。
- 增加个人文档目录、单文档删除和任务级证据范围选择，避免长期个人知识空间只能“全部保留或全部清空”。
- 为 BM25 分片增加启动重建或持久化，避免应用重启后暂时退化为仅向量检索。
- 如需保留旧版全局知识库内容，应由用户重新上传或重新加入报告；不自动把无空间 metadata 的历史记录分配给用户。

验收标准：

- A 用户上传、追加或清空知识库时，不会改变 B 用户的检索结果。
- 管理员可以查看每个用户的空间占用，并执行有审计记录的定向清理。

### 认证与 RBAC

当前状态：已有注册、登录、Bearer Token、`ADMIN` 角色和管理员后台。

待实现：

- 评估接入 Spring Security / 标准 JWT。
- 增加 token 刷新、退出登录服务端失效、密码重置。
- 拆分用户管理、报告管理、任务监控、系统配置等细粒度权限。

验收标准：

- 普通用户无法越权访问管理员接口或其他用户报告。

---

## P3：数据库与工程演进

### 版本化数据库迁移

当前状态：Flyway 已接入，空库使用 V1 初始化后依次执行到 V9，已有库使用 baseline 1 接管；V4～V7 已覆盖 Agent 轮次/工具、版本化状态、租约 fencing、Planner 遥测、canonical event outbox、投递 claim/退避/死信和 Planner-turn 关联，V8 增加研究任务客户端幂等键与用户级唯一约束，V9 增加证据证券归属和可比快照标识。Testcontainers/MySQL 8.4 用例已更新为预期 V9，但本轮因 Docker 未启动而跳过，V9 尚缺容器实跑通过证据。

待实现：

- 在 Docker 可用环境实跑现有 Testcontainers/MySQL 空库 `V1 -> ... -> V9` 用例。
- 准备旧版 V1 数据库，验证 baseline 后依次执行到 V9 且数据不丢失。
- 覆盖 `ResearchTaskMapper`、`ReportMapper`、`FinancialSnapshotMapper`、`CheckpointMapper` 的新字段映射与租户条件。

验收标准：

- 本地、测试和部署环境能自动应用数据库变更。

### 简历亮点增强方向

以下不是 MVP 必需，但适合作为后续项目亮点：

1. 金融评测趋势管理、更大 Golden Set 与独立 Judge 模型对照实验。
2. TuShare provider 缓存、限速和真实错误分类。
3. ECharts 行情序列与技术指标图。
4. 多空辩论 Agent 与证据绑定观点。
5. 跨实例 single-flight 与纯公开快照分级复用。
6. Docker Compose 一键启动 MySQL、Redis、ChromaDB、后端和前端。
