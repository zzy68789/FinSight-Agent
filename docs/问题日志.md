# 问题日志

> 结构：用户遇到的问题 -> 原因 -> 可选解决方案 -> 最终方案。每条都按 STAR 复盘，方便后续写项目经历或面试讲述。

| 编号 | 用户遇到的问题 | 原因 | 可选解决方案 | 最终方案 | STAR 复盘 / 结果 |
| --- | --- | --- | --- | --- | --- |
| 2026-07-03-01 | 通用调研 Agent 不能直接输入 A 股股票代码生成投研报告。 | 原 `/api/chat` 面向通用研究问题，缺少股票代码解析、金融数据快照、财务指标和引用审查语义。 | 1. 复用 `/api/chat` 拼 Prompt；2. 替换原通用链路；3. 新增独立股票报告链路。 | 新增 `/api/stock-reports` SSE 入口和 `com.zzy.drai.financial` 包，保留通用调研链路不变。 | S：需要从通用 Agent 改成 A 股投研系统；T：不能破坏原功能；A：新增 StockResolve -> DataSnapshot -> MetricEngine -> EvidenceCollect -> Writer -> Reviewer；R：后端 69 个测试通过。 |
| 2026-07-03-02 | 财务数字如果交给 LLM 计算，容易出现编造、口径不一致或无法复核。 | LLM 对关键财务指标没有确定性约束，且原系统只把证据作为文本片段传入。 | 1. Prompt 中要求模型不要编；2. 后处理审查；3. Java 指标引擎确定性计算。 | 新增 `FinancialMetricEngine`，用 `BigDecimal` 计算营收同比、毛利率、净利率、ROE、资产负债率、经营现金流/净利润；缺输入返回 `MISSING_INPUT`。 | S：报告必须可复核；T：禁止模型补写关键数字；A：把指标输入抽象成 `FinancialEvidenceItem`，由 Java 计算并输出公式；R：新增指标单元测试覆盖 OK 与缺失输入。 |
| 2026-07-03-03 | 公开行情、新闻或财报数据源不可用时，流程可能被外部依赖阻断。 | 本地环境未必配置金融 API、Tavily、ChromaDB 或已上传 PDF。 | 1. 强依赖外部 API；2. 直接失败；3. 降级为缺失证据并继续生成报告。 | `PublicMarketDataProvider` 捕获失败并写入 `DATA_MISSING` 证据，报告中显示缺失，不输出荐股或估值判断。 | S：MVP 要能本地演示；T：外部数据失败不阻断；A：provider 层降级，Reviewer 检查有效证据数量；R：流程可跑通且缺失项可追踪。 |
| 2026-07-03-04 | 计划要求后续能定位“数字错 / 引用错 / 逻辑错 / 信息过期”等 Bad Case。 | 原报告库只保存最终报告，缺少金融 snapshot、evidence、metric 与反馈绑定。 | 1. 只保存用户反馈文本；2. 保存完整任务日志；3. 保存快照回放数据。 | 新增 `stock_bad_case_feedback`，反馈时绑定 `snapshot_json`；新增 `/api/stock-reports/{taskId}/replay` 查看 snapshot + evidence + metric。 | S：需要持续优化评测与回放；T：定位问题来源；A：落表保存反馈和回放快照；R：前端新增四类反馈按钮和回放入口。 |
| 2026-07-03-05 | PowerShell 中直接执行 `mvn.cmd -Dtest=A,B test` 会把逗号解析成参数列表导致命令失败。 | PowerShell 对未加引号的逗号参数有特殊解析。 | 1. 换 shell；2. 单个测试逐个跑；3. 给 `-Dtest` 参数加引号。 | 使用 `mvn.cmd "-Dtest=StockCodeResolverTest,FinancialMetricEngineTest,CitationReviewerTest,StockReportControllerTest" test`。 | S：需要验证 RED 测试；T：区分命令写法错误和代码失败；A：加引号重跑；R：确认测试先因缺类失败，补实现后 10 个目标测试通过。 |
| 2026-07-03-06 | 用户更正了两个外部参考项目，需要重新判断哪些亮点能借鉴到当前 A 股投研 Agent。 | 之前参考项目不对应用户真正想看的项目，不能继续沿用旧结论；两个新项目分别偏 Python 金融团队工作流和 Java 多 Agent 财报报告。 | 1. 直接照搬外部项目；2. 只写分析不落地；3. 提取可迁移设计并限定边界。 | 新增 `docs/外部金融项目借鉴执行计划.md`，只借鉴多角色、fan-out、风险评分、合规 issue、可视化呈现，不引入 Python/yfinance、SEC、美股自动交易或仓位建议。 | S：参考项目发生更正；T：需要把借鉴点转成可执行计划；A：代码级检查两个仓库并映射到本项目模块；R：形成 P0/P1/P2 执行计划并完成 P0/P1 的核心实现。 |
| 2026-07-03-07 | 股票数据源采集如果串行执行，后续接入更多 provider 后会拖慢 SSE 首屏反馈。 | 原 `FinancialSnapshotBuilder` 按 provider 顺序采集，缺少每个数据源的阶段耗时和失败状态。 | 1. 保持串行；2. 全部交给 LLM 汇总；3. 用 `CompletableFuture` 并行采集并记录 stage result。 | `FinancialSnapshotBuilder` 改为 provider fan-out 并行，`FinancialSnapshot` 增加 `stageResults`，前端展示每个数据源状态、耗时和证据数。 | S：外部 Java 项目强调 DataCollector + Sentiment 并行；T：保持现有 provider 接口不破坏；A：并行采集并保留失败降级证据；R：新增并行测试证明两个慢 provider 总耗时低于串行。 |
| 2026-07-03-08 | 报告只有引用审查，缺少“不能保证收益、必须提示风险”的金融合规约束。 | `CitationReviewer` 关注证据和数字口径，不负责判断保证收益、内幕信息、免责声明等合规文本。 | 1. 只靠 Prompt 约束；2. 把合规揉进 CitationReviewer；3. 新增独立合规审查器。 | 新增 `FinancialComplianceReviewer` 和 `FinancialComplianceReviewResult`，Reviewer payload 返回 `compliance`，最终通过条件改成引用审查和合规审查都 PASS。 | S：投研报告需要明确边界；T：避免把研究辅助写成荐股承诺；A：本地规则检查免责声明、保证收益、内幕信息和引用失败；R：前端新增合规状态和 issue 列表，后端新增 4 个合规测试。 |
| 2026-07-03-09 | 风险评分中“未见重大利空”被错误识别成负面风险。 | 简单关键词命中先匹配了“利空”，没有处理中文否定和中性语义。 | 1. 引入复杂 NLP；2. 删除关键词判断；3. 先识别正向/中性短语，再识别负面词。 | `FinancialRiskScoringService` 调整为优先匹配“平稳、中性、未见重大利空”等中性/正向语义，缺失证据仍按中性偏谨慎给分。 | S：风险测试覆盖新闻摘要“未见重大利空”；T：避免假阳性风险；A：重排规则顺序并补测试；R：风险评分测试通过，报告能输出五维风险评分和原因。 |
| 2026-07-03-10 | `financial-eval-set.json` 只有样例定义，不能真正约束报告质量。 | 缺少加载评测集、计算指标、接入股票报告收尾阶段的评测 Module。 | 1. 保持文档样例；2. 手工检查报告；3. 新增自动评测服务并接入 SSE。 | 新增 `FinancialEvaluationService`、评测结果模型和 `evaluation` SSE 事件，命中默认样例时把评测结果纳入最终状态。 | S：需要让报告质量可回归；T：关键点、数字、引用和无依据表达都要可测；A：TDD 新增 4 个评测服务测试和工作流接入测试；R：评测指标可在前端和日志中展示。 |
| 2026-07-03-11 | 股票代码能解析交易所，但公司名称和行业仍显示“待识别”。 | 没有稳定本地主档，公开搜索失败时公司主体信息也会缺失。 | 1. 继续依赖搜索；2. 写死在报告模板；3. 建本地 A 股主档 provider。 | 新增 `AShareCompanyDirectory` 和 `AShareMasterDataProvider`，为 10 个评测样例股票输出公司名称、行业和 `LOCAL_CONTEXT` 证据。 | S：评测样例需要稳定主体识别；T：不能编造行情和估值；A：只补公司主档，不冒充实时数据；R：股票解析和 provider 单测通过。 |
| 2026-07-03-12 | 长流程任务和 provider 并发使用无界线程池或默认公共线程池。 | `ResearchTaskService`、`StockReportService` 内部 `newCachedThreadPool()`，`CompletableFuture.supplyAsync()` 未指定执行器。 | 1. 保持默认；2. 全部串行；3. 统一 Spring 管理有界执行器。 | 新增 `AsyncExecutionConfig`，注入 `workflowExecutor` 和 `financialProviderExecutor`，并用环境变量控制线程数。 | S：后续并发任务会增加；T：避免线程资源不可控；A：把执行资源移到配置层；R：快照并行测试和相关目标测试通过。 |
