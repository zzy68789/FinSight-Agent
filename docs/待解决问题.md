# 待解决问题

更新时间：2026-07-13

> 本文件记录已知的遗留问题、风险和需要收口的硬化项。
> 待实现功能路线见 `docs/待实现功能.md`，已完成能力见 `docs/目前已经实现的功能.md`，踩坑日志见 `docs/PROBLEM_LOG.md`。

## 优先级总览

| 优先级 | 类型 | 待解决问题 |
| --- | --- | --- |
| P1 | 数据源 | TuShare 真实 token 联调、缓存、限速与更多行情指标 |
| P1 | 评测 | 独立命令行批量评测报告与 CI 门控 |
| P1 | Reviewer | 报告数字全量抽取与逐项误差校验 |
| P1 | LLM | 使用用户真实 Key 完成端到端联调与生成模式观测 |
| P1 | 前端 | 抽出独立股票报告页 |
| P1 | 资产类型 | ETF 基金净值、规模、持仓、跟踪误差、申赎清单接入 |
| P1 | 可视化 | 前端 ECharts 行情序列与技术指标图表 |
| P1 | 多 Agent | 多空研究员辩论角色 |
| P1 | 执行资源 | 任务取消、provider 单项超时、执行队列观测 |
| P2 | 产品化 | 行业横向对比、同行估值、组合监控、预警 |
| P2 | 配置治理 | 统一 `.env.example` / 本地配置模板 |
| P2 | 运维 | Flyway + MyBatis Mapper 的 Testcontainers 自动化回归 |

---

## P1 待解决

### 数据源：TuShare 联调与硬化

当前状态：已新增 10 个评测样例股票的本地主档，并新增 `TushareMarketDataProvider`，可通过 `TUSHARE_API_KEY` 采集 TuShare 财务和日行情基础指标。

建议方案：用真实 TuShare token 联调 `income`、`balancesheet`、`cashflow`、`daily_basic`，补 provider 级缓存、限速、接口权限错误提示和更多行情序列指标。

### 评测：独立批量评测报告

当前状态：自动评测已能加载默认样例并输出指标（`FinancialEvaluator`、`evaluation` SSE 事件和 Maven 单测，覆盖 claim/citation/numeric/keypoint 指标），但还没有独立命令行批量评测报告。

建议方案：增加 CLI / 测试 runner 批量跑 10 个样例，输出 JSON / Markdown 评测报告，并把阈值作为 CI 门控。

### Reviewer：数字全量抽取校验

当前状态：`CitationReviewer` 已检查证据数量、公式引用、报告期和指标展示值，MVP 规则已覆盖核心链路，但还没有完整抽取报告所有数字并逐项比对。

建议方案：增加数字抽取器，按 `FinancialSnapshot` 与 `FinancialMetricResult` 做 0.5% 相对误差或 0.01 绝对误差校验。

### LLM：真实 Key 端到端联调

当前状态：`InvestmentReportWriter` 已接入 SMART `LlmClient`，并具备结构校验、确定性附录覆盖和模板回退测试；本机用户环境已设置 `API_KEY`，但当前运行中的后端启动早于本次同步，尚未重启并完成真实模型请求验证。

建议方案：由用户在本地启动配置中设置真实 Key 并重启后端，生成一条新报告，确认 Writer 耗时不再为 0、报告标记为 `generation-mode: llm`，同时复核 Reviewer、合规门控和服务端日志。

### 前端：独立股票报告页

当前状态：股票报告页目前集成在 Run 工作区，已有股票模式、指标卡、风险评分、合规状态、数据源耗时、证据分类、反馈、回放；2026-07-12 已完成统一的机构投研终端主题和响应式视觉优化，但还不是独立股票报告页。

建议方案：抽出独立股票报告页，增加证据列表详情、报告时间线筛选、风险维度筛选和多版本对比。

### 资产类型：ETF 深度数据

当前状态：已新增 `StockAssetType.ETF`，支持沪市 `5xxxxx` 和深市 `15xxxx/16xxxx/18xxxx` ETF 解析；ETF 不套普通股票财务指标，TuShare 优先走 `fund_daily`，报告模板改为 ETF 口径。基金净值、规模、持仓、跟踪误差和申赎清单还未完整接入。

建议方案：后续接入基金净值、规模、持仓、跟踪误差、申赎清单和跟踪指数信息，并按 token 权限增加 provider 级缓存、限速和接口权限错误提示。

### 可视化：ECharts 行情图表

当前状态：外部项目有 K 线、MACD、RSI 图表，本项目已接入 TuShare `daily_basic` 基础估值/市值证据，尚未落地日线序列和 ECharts 前端图表组件。

建议方案：后续接入 TuShare `daily`/`weekly` 行情序列后，用 ECharts 在前端展示价格、成交量、均线、RSI，不把图表生成放到 Python 脚本。

### 多 Agent：多空研究员辩论

当前状态：外部项目有多空研究员辩论和风险经理角色，本项目目前先做确定性风险评分与合规审查（`FinancialRiskScorer`、`FinancialComplianceReviewer` 和结构化报告分区）。

建议方案：新增 `BullBearDebateService`，要求每个论点绑定证据 ID，并由合规审查过滤荐股、仓位、保证收益表达。

### 执行资源：并发受控与观测

当前状态：已把股票报告和 provider fan-out 改为 Spring 管理的有界执行器；股票任务具备数据库心跳、最多 3 次恢复和 Micrometer 指标。仍缺任务主动取消、provider 单项超时和线程池队列指标。

建议方案：后续增加任务取消、provider 单项超时和执行队列观测指标。

---

## P2 待解决

### 产品化：研究辅助扩展

当前状态：尚未实现行业横向对比、同行估值区间、组合监控、价格/新闻/风险预警。不属于 MVP，当前明确不做荐股、交易或回测。

建议方案：在数据源稳定后，以只读研究辅助为边界逐步增加 ECharts 图表和订阅式提醒。

### 配置治理：本地配置模板

当前状态：项目不读取全局 `OPENAI_API_KEY`，LLM 使用用户指定的 `API_KEY`，其余 LLM 配置直接维护在 `application.yml`；README 已补充配置方式，并增加 `ConfigurationPlaceholderTest` 防止回退或引入多余环境变量占位符。但还没有统一 `.env.example` 或本地配置模板。

建议方案：后续增加 `.env.example` / `application-local.yml.example`，集中说明 LLM、Tavily、TuShare、MySQL、Redis、ChromaDB 的本地变量命名。

### 运维：数据库自动迁移回归

当前状态：Flyway 已接入，`V1__init.sql` 与 `V2__stock_report_reliability.sql` 分别覆盖空库初始化和本轮增量字段。持久层已迁移到 MyBatis，9 个 Mapper XML 已完成配置解析测试；2026-07-13 已在本机真实 MySQL `finsight` 库加载全部 Mapper 并启动成功，健康检查通过。当前仍缺少可重复执行的自动化数据库集成测试。

建议方案：增加 Testcontainers/MySQL 测试，分别验证空库 V1→V2、重复启动不重复迁移，以及任务、快照、报告新字段的 Mapper ResultMap、TypeHandler 和租户条件；旧项目数据库不再作为本项目升级来源。
