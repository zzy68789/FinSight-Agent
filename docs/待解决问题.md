# 待解决问题

更新时间：2026-07-17

> 本文件记录已知的遗留问题、风险和需要收口的硬化项。
> 待实现功能路线见 `docs/待实现功能.md`，已完成能力见 `docs/目前已经实现的功能.md`，踩坑日志见 `docs/PROBLEM_LOG.md`。

## 优先级总览

| 优先级 | 类型 | 待解决问题 |
| --- | --- | --- |
| P1 | 数据源 | TuShare 真实 token 联调、缓存、限速与更多行情指标 |
| P1 | 网页证据 | Tavily Extract 真实成功率、额度、超时和来源质量基线 |
| P1 | LLM | 重启应用完成真实任务回归与生成延迟基线观测 |
| P1 | 前端 | 独立研究页补段落级 Bad Case、风险/时间线筛选 |
| P1 | 资产类型 | ETF 持仓、跟踪误差、申赎清单及深度接口权限硬化 |
| P1 | 可视化 | 普通股票行情序列、均线/RSI 与图表区间切换 |
| P1 | 多 Agent | 风险裁判、证据冲突仲裁与真实 Bad Case 扩充 |
| P1 | 执行资源 | 任务取消、跨实例并发合并与纯公开快照跨用户复用安全设计 |
| P2 | 评测 | 扩充真实 Golden Set、Judge/性能基线与趋势管理 |
| P2 | 产品化 | 行业横向对比、同行估值、组合监控、预警 |
| P2 | 配置治理 | 统一 `.env.example` / 本地配置模板 |
| P2 | 知识库持久化 | BM25 启动重建、旧全局 Chroma 数据重新入库与管理员空间运营能力 |
| P2 | 运维 | 在 Docker 可用 CI 实跑 MySQL Testcontainers，并扩展 Mapper/旧库 baseline 覆盖 |

---

## P1 待解决

### 数据源：TuShare 联调与硬化

当前状态：已新增 10 个评测样例股票的本地主档，并新增 `TushareMarketDataProvider`，可通过 `TUSHARE_API_KEY` 采集 TuShare 财务和日行情基础指标。连接/读取 timeout 已真正应用到 HTTP 客户端，Provider 聚合也有统一超时、取消与 `DATA_MISSING` 降级；缓存、限速和真实错误分类仍未完成。

建议方案：用真实 TuShare token 继续复核贵州茅台营收同比 6.54% 与 ROE 10.57%；随后补 provider 级缓存、限速、接口权限错误提示和更多行情序列指标。

### 网页证据：Tavily Extract 联调与质量基线

当前状态：已写入 `TavilyExtractClient`、候选 URL 优先级、批量正文提取、合格摘要降级和公开网页质量门控；配置为最多 3 个 URL、单次 10 秒、网络错误或 5xx 最多重试 1 次。相关单元测试已随 2026-07-15 后端 128 项全量回归通过，但尚未运行真实 Tavily 调用，不能确认不同财经站点的提取成功率、额外 P95 延迟和 credits 消耗。

建议方案：使用 `600519` 混合检索记录 Search/Extract 的成功 URL、失败 URL、耗时和最终有效新闻数量；至少覆盖交易所公告、普通财经正文、登录页和行情导航页，再决定是否增加域名白名单或 advanced extraction。

### 评测：真实 Golden Set 与模型基线

当前状态：离线评测 v1 已实现 20 个冻结报告样例、24 个检索标注、稳定 `chunkId`、Recall/Precision/MRR/NDCG、JSON/Markdown 产物、显式基线更新和 PR 强门禁；Judge 与真实股票/ETF 冒烟有独立手动/定时工作流，缺 Key 时明确 `SKIPPED`。当前冻结样例主要用于机制回归，尚不能代表真实线上准确率；Judge、P95 延迟和 Token 也还没有积累可审查的非零历史基线。

建议方案：从已确认的 Bad Case 中脱敏沉淀跨行业、跨报告期 Golden Set；连续运行 Judge/真实数据工作流后再提交模型质量、P95 和 Token 基线，并在数据量足够时评估趋势存储与管理页面。

### LLM：应用内端到端回归与延迟基线

当前状态：真实 `API_KEY`、DashScope 兼容地址和 `qwen3.7-max` 已完成延迟联调，压缩后真实请求为 50.859 秒。Writer 增加最多 18 条紧凑有效证据索引和正文就近引用要求后，相关代码已随 2026-07-15 后端 128 项全量回归通过；真实应用仍需重启并重新建立延迟与引用质量基线。

建议方案：重启后端后用 `600519` 混合检索连续生成报告，确认 `generation-mode: llm`、正文关键指标带对应 `[E#]`、一季度 ROE 标注未年化、低质量网页不进入正文，同时比较新旧 Writer 的 P50/P95；若降级，数据库应记录 `DEGRADED` 和稳定原因分类。

### 前端：独立股票报告页

当前状态：2026-07-17 已新增 `/reports/:reportId` 独立研究页，支持版本时间带、逐行对比、证据详情与来源筛选、`[E#]` 锚点、ETF 行情图和多空论据展示；报告库已有独立入口，生产构建通过。

建议方案：下一步只补段落/证据旁 Bad Case、风险维度筛选和长时间线筛选，并为逐行差异增加章节摘要。

### 资产类型：ETF 深度数据

当前状态：已新增 `StockAssetType.ETF`，支持沪市 `5xxxxx` 和深市 `15xxxx/16xxxx/18xxxx` ETF 解析；ETF 不套普通股票财务指标。TuShare 已聚合 `fund_daily`、`fund_basic`、`fund_nav`，保存 60 日行情、基金资料、业绩基准、单位/累计净值、资产净值和同日折溢价。持仓、跟踪误差和申赎清单还未接入，`total_netasset` 的展示单位也需真实 token 复核。

建议方案：后续接入持仓、跟踪指数收益、跟踪误差和申赎清单；按 token 权限增加 provider 级开关、缓存、限速和接口权限错误提示。

### 可视化：ECharts 行情图表

当前状态：ETF 已落地 60 日行情序列和按路由懒加载的 ECharts 组件，展示 K 线、收盘线、成交额和单位净值基准；无数据时有明确空状态。普通股票仍只有 `daily_basic` 估值/市值证据。

建议方案：后续为普通股票接入 TuShare `daily`/`weekly`，并增加均线、RSI 与区间切换；继续保持 Java/前端实现，不引入 Python 图表链路。

### 多 Agent：多空研究员辩论

当前状态：已实现 `BullBearResearchAgent` 首版，以确定性多头/空头角色读取同一快照，输出最多各 3 条证据约束论据；结构化结果进入 SSE、步骤日志和报告第 8 节，并继续通过引用、合规和 Evaluation 门禁。

建议方案：补风险裁判和证据冲突仲裁，扩充真实多空 Bad Case；若后续让 LLM 优化表达，仍必须保留 Java 数字边界、证据编号校验和现有 fail-closed 门禁。

### 执行资源：并发受控与观测

当前状态：股票报告和 provider fan-out 已使用固定线程数 + 有界队列，并暴露 active/queued/rejected 指标；Provider 有统一超时，TuShare 有连接/读取超时。步骤日志、Checkpoint 与任务租约心跳已由 `WorkflowStagePersistence` 单事务提交，Redis 只在提交后刷新。`ReportReuseCoordinator` 已收敛同一实例、同一用户下的历史复用和 single-flight，并设置有限等待；当前仍缺任务主动取消、跨实例并发合并，缓存与 single-flight 仍严格按用户隔离。

建议方案：后续增加任务主动取消；多实例部署时使用数据库唯一占位或 Redis 分布式 single-flight。若要让纯公开数据报告跨用户复用，必须先增加快照公开性分级、私有证据零混入校验、报告归属复制和跨租户安全回归，不能直接移除 `ownerId` 条件。

---

## P2 待解决

### 产品化：研究辅助扩展

当前状态：尚未实现行业横向对比、同行估值区间、组合监控、价格/新闻/风险预警。不属于 MVP，当前明确不做荐股、交易或回测。

建议方案：在数据源稳定后，以只读研究辅助为边界逐步增加 ECharts 图表和订阅式提醒。

### 配置治理：本地配置模板

当前状态：项目不读取全局 `OPENAI_API_KEY`，LLM 使用用户指定的 `API_KEY`，其余 LLM 配置直接维护在 `application.yml`；README 已补充配置方式，并增加 `ConfigurationPlaceholderTest` 防止回退或引入多余环境变量占位符。但还没有统一 `.env.example` 或本地配置模板。

建议方案：后续增加 `.env.example` / `application-local.yml.example`，集中说明 LLM、Tavily、TuShare、MySQL、Redis、ChromaDB 的本地变量命名。

### 知识库持久化：空间重建与运营

当前状态：PDF 上传、报告加入 RAG、BM25、内存向量库和 ChromaDB 已按 `RagKnowledgeSpace` 完成用户隔离；旧版未带 `knowledge_space` metadata 的全局记录默认不可见，避免自动误归属。BM25 分片仍只保存在进程内，应用重启后会暂时退化为 Chroma 向量检索；管理员后台也尚未展示各用户知识空间占用。

建议方案：从可审计的文档目录或索引清单启动重建 BM25，增加用户级文档数量、分片数量和最近更新时间统计；旧全局数据只允许通过用户主动重新上传或重新加入报告迁移，不做自动归属。

### 运维：数据库自动迁移回归

当前状态：已新增 `MySqlPersistenceIntegrationTest` 与 Testcontainers MySQL 8.4 依赖，覆盖空库 Flyway V1→V3、Checkpoint JSON 往返、任务租约 CAS、PASS 报告 owner 隔离/软删除和阶段事务回滚；本轮开发机 Docker 未启动，因此该用例在 156 项全量测试中按设计跳过，尚无容器实跑通过证据。`FinancialSnapshotMapper` 全量 JSON/ResultMap、旧 V1 baseline 升级和重复启动幂等仍未覆盖。

建议方案：在 Docker 可用的本机或 CI 实跑现有测试，再扩展重复启动、旧 V1 baseline、`FinancialSnapshotMapper` JSON/ResultMap 和更多租户条件；正式 CI 同时执行架构与中文注释规范检查。旧项目数据库不再作为本项目升级来源。
