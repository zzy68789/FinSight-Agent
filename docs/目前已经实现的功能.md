# 目前已经实现的功能

更新时间：2026-07-10

> 待实现清单见 `docs/待实现功能.md`，遗留问题见 `docs/待解决问题.md`，踩坑日志见 `docs/PROBLEM_LOG.md`。
> 本项目已聚焦为 **A股/ETF 金融投研报告 Agent**，原通用 deep-research 编排链路已从本仓库移除（仍保留在 DRAI 主仓库）。

## 证券代码研究报告 MVP

- 新增 `POST /api/stock-reports` SSE 入口，支持 `ticker`、`thread_id`、`report_period`、`search_mode`。
- 证券代码解析支持普通 A 股 `6xxxxx -> .SH`、`0xxxxx / 2xxxxx / 3xxxxx -> .SZ`，以及常见 ETF `5xxxxx -> .SH`、`15xxxx / 16xxxx / 18xxxx -> .SZ`。
- 不支持的代码会通过 SSE `error` 事件返回明确业务原因，不再在前端泛化为“网络异常”；当前仍不支持 B 股、债券或北交所标的。
- 新增金融数据模型：`StockSubject`、`FinancialEvidenceItem`、`FinancialSnapshot`、`FinancialMetricResult`。
- 新增 `FinancialDataProvider` 扩展点，已实现 `UploadedReportProvider`、`PublicMarketDataProvider` 和 `TushareMarketDataProvider`。
- 新增 `AShareCompanyDirectory` 和 `AShareMasterDataProvider`，为 10 个默认评测样例股票补齐公司名称、行业和 `LOCAL_CONTEXT` 证据。
- LLM 配置使用项目专用环境变量 `DRAI_LLM_BASE_URL`、`DRAI_LLM_API_KEY`、`DRAI_LLM_FAST_MODEL`、`DRAI_LLM_SMART_MODEL` 等，不读取全局 `OPENAI_API_KEY`，避免影响 `cc-switch` 等全局配置。
- 新增 TuShare Pro 授权数据源配置，支持 `DRAI_TUSHARE_ENABLED`、`DRAI_TUSHARE_API_KEY`、`DRAI_TUSHARE_BASE_URL`、`DRAI_TUSHARE_TIMEOUT`；未配置 token 时不调用外部接口。
- `TushareMarketDataProvider` 已能把普通股票的 TuShare `income`、`balancesheet`、`cashflow`、`daily_basic` 响应映射为营业收入、营业成本、归母净利润、资产负债、经营现金流、市盈率、市净率和总市值证据；ETF 标的优先走 `fund_daily` 并映射收盘价、涨跌幅和成交额证据。
- `FinancialSnapshotBuilder` 已支持 provider fan-out 并行采集，并为每个数据源记录状态、耗时、证据数量和失败信息。
- 股票报告任务和金融 provider fan-out 使用 Spring 管理的有界执行器，线程数可通过 `DRAI_FINANCIAL_PROVIDER_THREADS` 配置。
- 公开数据失败时不会阻断流程，会写入 `DATA_MISSING` 证据。
- 新增 `FinancialMetricEngine`，普通股票用 Java `BigDecimal` 计算营收同比、毛利率、净利率、ROE、资产负债率、经营现金流 / 净利润；ETF 不套用公司财务指标，只输出 ETF 收盘价、涨跌幅和成交额等行情指标。
- 新增 `MetricDefinitionCatalog`，集中声明指标公式、`formulaVersion` 和输入依赖；`stock_metric_result.formula_version` 保存实际使用的公式版本。
- 缺少分子或分母时返回 `MISSING_INPUT`，报告中显示“数据缺失”。
- 新增 `FinancialRiskScoringService`，按基本面 30%、技术面 25%、情绪面 20%、消息面 15%、市场环境 10% 输出五维风险评分、风险等级和缺失证据 warning。
- 新增 `FinancialComplianceReviewer`，检查免责声明、保证收益、内幕信息和引用审查失败，并输出结构化 `issues` 与合规分数。
- 新增 `FinancialEvaluationService`，加载 `financial-eval-set.json` 并输出 claim/citation/numeric/keypoint 自动评测结果。
- 新增独立金融工作流：`StockResolve -> DataSnapshot -> MetricEngine -> RiskAssessment -> EvidenceCollect -> InvestmentWriter -> CitationReviewer + ComplianceReviewer -> Evaluation`。
- 普通股票报告和 ETF 报告均固定八章节，并在底部输出“引用与数据快照”和指标公式；ETF 报告使用基金概况、跟踪标的、行情、流动性、净值/持仓缺口等基金口径章节。
- 新增 `CitationReviewer`，检查有效证据数量、报告期混用、指标证据引用和指标值展示；最终通过条件为引用审查与合规审查同时 PASS。
- 报告统一标注“仅作研究辅助，不构成投资建议”。

## 工作流可靠性与报告复现

- `StockReportService` 已拆分为异步入口和可同步测试的 `StockReportRunner`；SSE 断开只关闭进度通道，不再把后台任务误标为失败。
- `research_task` 已记录 `stage`、`attempt_count`、`request_payload`、`heartbeat_at`、数据库租约和最近错误；`StockReportRecoveryScheduler` 每 60 秒扫描超过 5 分钟无心跳的股票报告任务，最多执行 3 次。
- 新增 `POST /api/stock-reports/{taskId}/retry`，只允许任务所属用户重试未耗尽次数的 `FAILED` 任务。
- 恢复执行会优先复用当前任务已保存的 snapshot、metric 和 report，避免重复采集与重复落库。
- `FinancialReportFingerprintService` 对稳定业务字段生成 `data_snapshot_hash`，并结合公式目录、Writer、Citation、Compliance、Evaluation 规则版本生成 `generation_context_hash`。
- 同一用户下只有 `generation_context_hash` 相同、评审为 `PASS` 且未删除的报告允许复用；新任务仍创建报告版本并记录 `reused_from_report_id`，不跨用户共享缓存。

## RAG Trace 与可观测性

- `HybridRagRetriever.retrieveWithTrace` 输出关键词分数、向量分数、融合分数、通道、排名、候选数、过滤数和耗时；原 `retrieve` 接口保持兼容。
- 上传报告检索 trace 随 `FinancialSnapshot` 持久化，并通过 `agent_step_log` 绑定到具体任务；步骤日志新增 `attempt_no` 和 `duration_ms`。
- `FinancialEvaluationService` 在原 claim/citation/numeric/keypoint 指标上增加 `evidence_effective_rate`；`unsupported_claim_rate` 仍是规则型无依据断言检查，不作为完整幻觉检测宣传。
- 新增 `GET /api/stock-reports/{taskId}/trace`，聚合任务阶段、报告版本、双哈希、缓存来源、证据统计、检索 trace、步骤耗时和 Reviewer 摘要。
- 前端回放区域已改为报告可信度面板，展示证据有效率、混合检索通道、步骤耗时、快照哈希和缓存命中情况。
- 已接入 Spring Boot Actuator、Micrometer Prometheus；暴露 `/actuator/health`、`/actuator/prometheus`，记录任务结果、阶段耗时、缓存命中和恢复结果。

## 前端投研工作台

- 前端 Run 工作区提供“证券代码分析”模式、证券代码输入、报告期选择、金融执行轨迹。
- 前端提供财务指标卡、风险评分进度条、五维风险明细、合规状态、合规 issue 列表、provider 执行耗时、证据分类、证据数量和缺失项数量展示。
- Bad Case 反馈接口和前端按钮：数字错、引用错、逻辑错、信息过期。
- 股票报告回放接口和前端回放面板，返回 snapshot + evidence + metric。
- 证券代码模式前端已展示自动评测门控状态、综合分、单项评测指标和失败原因。

## 复用的通用基础设施

金融链路复用以下基础能力（这些能力保留在本仓库，但不再对外暴露通用 deep-research 入口）：

- RAG：PDF 上传、文本切片、embedding、BM25 + ChromaDB 向量混合检索（`rag` 包），用于上传年报/季报作为证据来源。
- 联网搜索：`SearchSource` 抽象 + Tavily 适配器 + 失败重试/去重/质量过滤/fallback（`search` 包）。
- 持久化：任务、Agent 步骤日志、报告版本、checkpoint 的 JDBC 持久化（`repository` 包）。
- 报告库：报告列表、版本、导出（PDF/Word/Markdown）、收藏、软删除、追加入 RAG（`ReportService` / `ReportExportService`）。
- 用户与后台：注册/登录、Bearer Token 鉴权、`owner_id` 数据隔离、Admin 后台与审计日志。
- LLM 基础设施：`LlmConfig`（fast/smart ChatModel bean）、`LlmClient`、`RetryingLlmClient`（重试后抛出，由调用方降级），为后续给报告撰写接入 LLM 预留。
- SSE 实时推送：`SseService` 统一出口。

## 评测体系

- `backend/src/main/resources/financial-eval-set.json` 包含 10 个 A 股评测样例和 6 个评测指标名称。
- `FinancialEvaluationService` 输出 claim/citation/numeric/keypoint 自动评测结果，作为报告最终 PASS/FAIL 门控的一部分。

## 数据库

- 新增表：`stock_analysis_snapshot`、`stock_evidence_item`、`stock_metric_result`、`stock_bad_case_feedback`。
- 已引入 Flyway：`V1__init.sql` 负责空库初始化，`V2__stock_report_reliability.sql` 增加 trace、公式版本、双哈希和恢复字段；已有库通过 baseline 1 接管。
- `schema.sql` 保留当前完整结构参考，Spring SQL Init 已关闭；手动升级脚本 `upgrade-stock-report-observability.sql` 与 V2 内容保持一致。

## 验证记录

- 后端：2026-07-10 执行 `mvn.cmd test`，结果为 90 tests, 0 failures, 0 errors。
- 前端：2026-07-10 执行 `npm.cmd run build`，Vite 生产构建通过。
- 聚焦测试覆盖三轮重写、SSE 断连、异常落库、缓存命中、哈希稳定性、RAG trace 和恢复次数上限。
- 本机 Docker 与 MySQL 未运行，Flyway 的真实 MySQL/Testcontainers 集成验证尚未执行，保留在待解决问题中。
