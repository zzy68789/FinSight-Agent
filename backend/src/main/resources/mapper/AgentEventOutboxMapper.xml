<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzy.finsight.mapper.AgentEventOutboxMapper">
    <resultMap id="eventResult" type="com.zzy.finsight.agent.event.AgentEvent">
        <constructor>
            <idArg column="id" javaType="_long"/>
            <arg column="schema_version" javaType="string"/>
            <arg column="task_id" javaType="_long"/>
            <arg column="thread_id" javaType="string"/>
            <arg column="sequence_no" javaType="_long"/>
            <arg column="event_id" javaType="string"/>
            <arg column="turn_no" javaType="_int"/>
            <arg column="event_type" javaType="string"/>
            <arg column="status" javaType="string"/>
            <arg column="payload_json" javaType="java.util.Map" typeHandler="com.zzy.finsight.infrastructure.persistence.mybatis.typehandler.MapJsonTypeHandler"/>
            <arg column="error_message" javaType="string"/>
            <arg column="duration_ms" javaType="_long"/>
            <arg column="published_at" javaType="java.time.LocalDateTime"/>
            <arg column="created_at" javaType="java.time.LocalDateTime"/>
        </constructor>
    </resultMap>

    <insert id="insertEvent" parameterType="map" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO agent_event_outbox(
          schema_version, task_id, thread_id, sequence_no, event_id, turn_no,
          event_type, status, payload_json, error_message, duration_ms, created_at
        ) VALUES (
          #{schemaVersion}, #{taskId}, #{threadId}, #{sequence}, #{eventId}, #{turnNo},
          #{eventType}, #{status},
          #{payload,typeHandler=com.zzy.finsight.infrastructure.persistence.mybatis.typehandler.ObjectJsonTypeHandler},
          #{errorMessage}, #{durationMs}, #{createdAt}
        )
    </insert>

    <select id="findByTaskId" resultMap="eventResult">
        SELECT id, schema_version, task_id, thread_id, sequence_no, event_id, turn_no,
               event_type, status, payload_json, error_message, duration_ms, published_at, created_at
        FROM agent_event_outbox
        WHERE task_id = #{taskId}
        ORDER BY sequence_no ASC
    </select>

    <select id="findUnpublished" resultMap="eventResult">
        SELECT id, schema_version, task_id, thread_id, sequence_no, event_id, turn_no,
               event_type, status, payload_json, error_message, duration_ms, published_at, created_at
        FROM agent_event_outbox
        WHERE published_at IS NULL
        ORDER BY id ASC
        LIMIT #{limit}
    </select>

    <update id="markPublished">
        UPDATE agent_event_outbox SET published_at = #{publishedAt}
        WHERE id = #{id} AND published_at IS NULL
    </update>
</mapper>
