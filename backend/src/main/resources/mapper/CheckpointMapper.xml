<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzy.finsight.mapper.CheckpointMapper">
    <insert id="insert">
        INSERT INTO checkpoint(thread_id, task_id, state_json, created_at)
        VALUES (
          #{threadId},
          #{taskId},
          #{state,typeHandler=com.zzy.finsight.infrastructure.persistence.mybatis.typehandler.ObjectJsonTypeHandler},
          #{createdAt}
        )
    </insert>
</mapper>
