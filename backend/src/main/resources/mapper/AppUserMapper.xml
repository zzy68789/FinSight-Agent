<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzy.finsight.mapper.AppUserMapper">
    <resultMap id="appUserResult" type="com.zzy.finsight.domain.AppUserRecord">
        <constructor>
            <idArg column="id" javaType="_long"/>
            <arg column="username" javaType="string"/>
            <arg column="email" javaType="string"/>
            <arg column="password_hash" javaType="string"/>
            <arg column="role" javaType="string"/>
            <arg column="status" javaType="string"/>
            <arg column="created_at" javaType="java.time.LocalDateTime"/>
            <arg column="updated_at" javaType="java.time.LocalDateTime"/>
            <arg column="last_login_at" javaType="java.time.LocalDateTime"/>
        </constructor>
    </resultMap>

    <insert id="insertUser" parameterType="map" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO app_user(username, email, password_hash, role, status, created_at, updated_at)
        VALUES (#{username}, #{email}, #{passwordHash}, #{role}, 'ACTIVE', #{createdAt}, #{updatedAt})
    </insert>

    <select id="findByUsername" resultMap="appUserResult">
        SELECT id, username, email, password_hash, role, status, created_at, updated_at, last_login_at
        FROM app_user
        WHERE username = #{username}
    </select>

    <select id="findById" resultMap="appUserResult">
        SELECT id, username, email, password_hash, role, status, created_at, updated_at, last_login_at
        FROM app_user
        WHERE id = #{userId}
    </select>

    <update id="updateLoginTime">
        UPDATE app_user
        SET last_login_at = #{now}, updated_at = #{now}
        WHERE id = #{userId}
    </update>
</mapper>
